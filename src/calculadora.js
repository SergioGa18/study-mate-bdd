// Clase Calculadora. Las operaciones se irán implementando siguiendo BDD (red-green-refactor).
class Calculadora {
  sumar (a, b) {
    return a + b
  }

  restar (a, b) {
    return a - b
  }
}

module.exports = Calculadora
